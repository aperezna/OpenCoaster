import React from 'react';
import { View, Text } from 'react-native';

export function DiscoveryScreen(): React.JSX.Element {
  return (
    <View testID="discovery-screen">
      <Text>Discovery</Text>
    </View>
  );
}
